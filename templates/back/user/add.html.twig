{% extends 'back/base.html.twig' %}

{% block userActive %}active{% endblock %}

{% block title %} Ajouter un administrateur {% endblock %}

{% block main %}
	<div class="container">
		<h2 class="text-center my-5 fw-bold display-5">Ajouter un administrateur</h2>
		<div class="row justify-content-center">
			<div class="col-md-8">
				{# User List Link #}
				<a href="{{ path('app_back_user_browse') }}" class="btn btn-primary fw-bold mb-4">
					<i class="bi bi-arrow-left"></i>
					Retour à la liste des administrateur
				</a>
				{# Flash Messages Fragment #}
				{{ include('back/fragments/_message.html.twig') }}
				<div class="card">					
					<div class="card-header fs-2 fw-bold">
						<i class="bi bi-person-plus me-2"></i>Nouvel administrateur
					</div>
					<div class="card-body">
						{# Add User Form #}
						{{ include('back/fragments/_form.html.twig', {'button_label': 'Ajouter l\'administrateur'}) }}
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
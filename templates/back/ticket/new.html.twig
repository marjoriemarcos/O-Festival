{% extends 'back/base.html.twig' %}

{% block title %}Nouveau billet{% endblock %}

{% block main %}
    <main>
        <div class="container">
            <h2 class="text-center my-5 fw-bold display-5">Créer un nouveau billet</h2>
            <div class="row justify-content-center">
                <div class="col-md-8">
                    <a href="{{ path('app_ticket_list_admin') }}" class="btn btn-primary fw-bold mb-4">
                        <i class="bi bi-arrow-left"></i> Retour à la liste des billets
                    </a>
                    <div class="card">
                        <div class="card-header fs-2 fw-bold">
                            <i class="bi bi-person-plus me-2"></i>Nouveau billet
                        </div>
                        <div class="card-body">
                            {{ include('back/ticket/_form.html.twig', {'button_label': 'Créer le billet'}) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>
{% endblock %}
